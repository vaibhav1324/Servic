import React from 'react';

import { Flex } from '@chakra-ui/react';

import { ErrorGeneratedProps } from './Error.props';

const ErrorView = (props: ErrorGeneratedProps): JSX.Element => (
  <Flex>Error</Flex>
);

export default ErrorView;
